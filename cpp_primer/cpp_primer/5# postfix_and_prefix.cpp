#include <iostream>

int main()
{
	//the different between postfix and prefix is that postfix as a paramter int, in contrast  prefix don't.

	//The int parameter is not used, so we do not give it a name.
	return 0;
}