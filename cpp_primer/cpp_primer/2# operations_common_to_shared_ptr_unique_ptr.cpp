#include <iostream>

int main()
{
	//[common operations for unique and shared pointer]
	//1- operator overloading to bool, return true or false.
	//2- * dereferencing to return the object.
	//3- -> arrow to return the member synonym to (*ptr).mem.
	//4- get() to get the pointer
	//5- reset() to reset the pointer.
	//swap() function to swap between to pointers
	//swap() member function which is used to swap between the object and pointer which is passed as argument.


	return 0;
}