#include <iostream>

int main()
{
	/*
	 * input iterators provide.
	 * (== and !=), (++), (-> and *).
	 */

	/*
	 * output iterators provide
	 * dereferences but it appears only in the left.
	 * increment whether it is postfix or prefix.
	 */

	 /*
	  * forward iterators provide
	  * input and output iterators operations.
	  */

	  /*
	   * bidirectional iterators support
       * forward iterators besides it has postfix and prefix decrement.
       */

	   /*
		* random-access iterators iterators support
		* constant-time access to any position
		* any operation to bidirectional iterators
		* relational operations between two iterators (<, <=, >, and >=)
		* Addition and subtraction operators (+, +=, -, and -=)
		* subscript operator (iter[n]) as a synonym for * (iter + n)
		*/

	return 0;
}